import * as request from "supertest";
import * as app from "../app/app";

// describe("/", () => {
//     it("when get / then response body is hello", (res: any) => {
//         request(app)
//             .get("/")
//             .expect(200, res)
//             .expect("{\"message\":\"this is eve-linebot.\"}", res.body);
//     });
// });
